针对论文大纲**第三章“多层降噪筛选”\**的要求，目前的总结文档（来源-）已经涵盖了核心框架，但在\**算法细节、动态阈值的实现逻辑、以及具体的规则定义**上仍显不足。

为了完成第三章的深度分析，我将针对大纲中提到的“借阅行为分析”、“动态降噪策略”和“睡美人唤醒机制”，分模块向您提出需要补充的问题。请您在阅读代码后给出针对性回答。

### 模块 1：借阅统计分析模块（对应大纲 3.1 借阅行为分析）

当前文档提到了统计近三月数据和使用`groupby`算法，但对于大纲要求的“常温书”与“降温书”定义细节不足。

1. **“常温”与“降温”的定量定义**：代码中是如何通过近三个月的借阅次数（第一月、第二月、第三月）来判定一本书属于“常温书”还是“降温书”的？是否存在具体的数学公式或趋势判断逻辑（例如：月度借阅量逐月递减且维持在一定区间）？
2. **唯一借阅人数的去重细节**：在大纲中强调了“精准提取”，代码中除了使用`groupby`，是否处理了特定的异常情况（如同一读者同日多次借还、续借行为是否计入次数）？
3. **统计时间窗口的灵活性**：代码中“近三月”的时间窗口是硬编码的，还是可以根据数据分布动态调整的？

### 模块 2：智能降噪筛选模块（对应大纲 3.2 动态降噪策略）

当前文档提到了15%分位数和正则表达式，但缺乏具体的配置项和执行优先级描述。

1. **动态分位数算法实现**：代码中如何计算“前15%”的热门书阈值？它是针对全库数据计算，还是针对特定分类（CLC号）分别计算？
2. **正则表达式的具体过滤清单**：在“题名关键词匹配”和“索书号模式匹配”中，代码目前预设了哪些过滤规则？（例如：是否过滤了教材、教辅、考试用书等特定类型的正则）
3. **业务查重逻辑**：在过滤“已推荐数据库”时，代码是如何定义“重复”的？（仅基于ISBN，还是基于书名+作者的模糊匹配？）
4. **智能过滤原因追踪**：文档提到“智能过滤原因生成”，代码中是如何记录并存储这些原因的？是否有一个结构化的字典来映射过滤规则和对应理由？

### 模块 3：新书零借阅筛选模块（模块 6，对应大纲 3.3 睡美人唤醒机制）

当前文档提到了“更宽松的阈值”和“LLM智能筛选”，但这部分是大纲 3.3 的核心，需要更多技术细节。

1. **“新书”的时间限定**：代码中如何定义“近期验收”？具体的出版年过滤逻辑（如：当前年份 - 出版年 <= N）是怎样的？
2. **宽松评分阈值的定量描述**：相对于普通书，针对“睡美人”图书，豆瓣评分或其他质量指标的准入门槛具体降低了多少？
3. **无评分书籍的 LLM 提示词逻辑**：当书籍没有豆瓣评分时，代码调用 LLM 进行“三层判断”的具体 Prompt（提示词）逻辑是什么？它主要考察书籍的哪些维度（如：作者知名度、出版社权威性、主题时效性）？
4. **“零借阅”状态的验证**：系统如何确保该书是真的“零借阅”而非数据缺失？是否涉及对验收日期与统计日期间隔的逻辑判断？

### 模块 4：主题书目推荐/AIBot 模块（涉及大纲 3.2 深度过滤）

虽然这部分偏向第四章，但在第三章的“降噪”语境下，需要确认其在数据输入阶段的预处理逻辑。

1. **RSS 源的质量预过滤**：在主题书目模块的“阶段 3 (filter)”中，LLM 初筛的具体判别准则是什么？除了“pass/reason”，它是否对文章的“学术性”或“深度”有特定的过滤权重？
2. **向量检索前的降噪**：在将文章与图书进行关联前，是否对文章内容进行了去广告、去杂质的清洗处理？使用了哪些技术手段？

------

**补充说明**： 以上问题旨在挖掘代码中隐藏的**算法逻辑**和**规则细节**。您在阅读代码时，可以重点关注 `config/` 目录下的配置文件以及各模块中带有 `filter`、`rule`、`stats` 命名的类或函数。这些细节将使论文第三章的内容从“描述性总结”提升为“技术性论证”。

**类比**：如果第三章是“智慧采金”的**筛选阶段**，我现在需要您深入矿场，告诉我那个筛子的**孔径（阈值）**到底是多少毫米，以及那个**化学试剂（正则规则/LLM判别式）**的具体成分是什么。