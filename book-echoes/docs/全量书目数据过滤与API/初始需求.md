这个脚本是独立运行不用集成在主程序中，存储在 src\scripts。跟这个脚本有关的配置（config路径）也单独新建一个yaml文件。


根据提供的excel数据（配置中添加绝对路径），调用数据过滤条件（配置中增加绝对路径），然后脚本根据这个配置条件，过滤数据。符合条件的数据，保存到新excel中（文件名为 `数据筛选结果_时间戳`），被过滤的数据，保存到另一个excel中（`被过滤数据_时间戳`）（结果excel的存储路径也是配置中添加的）。

关于根据配置txt过滤条件的处理逻辑是可以直接调用另一个项目的代码。

过滤的逻辑代码路径是：src\utils\rule_file_parser.py"，共享的配置文件在\config\filters"
这个脚本可以独立运行，不依赖于主程序。