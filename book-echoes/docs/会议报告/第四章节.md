针对您的论文大纲**第四章“AI驱动的深度评选与知识发现”**，我分析了现有四个模块的总结文档。

目前，文档在**三Agent架构**、**三阶段评选**和**聚类分析**的宏观流程描述上是足够的。但是，要达到论文所需的深度，在**具体的算法实现逻辑、Prompt设计策略以及“知识发现”的技术细节**（如向量检索的具体实现、配额算法的数学逻辑）上仍需通过查阅代码进行补充。

以下是为您整理的需要补充的问题，按模块分类：

### 模块 1：借阅统计分析与图书评选模块（侧重图书评选算法）

1. **锦标赛模式的具体规则**：在代码中，三阶段评选（初评、决选、终评）的每一阶段，其**筛选比例或打分阈值**是如何设置的？
2. **配额自适应算法 (Quota Adaptive Algorithm)**：请查阅代码，说明该算法如何根据不同主题的数据量**动态调整评选名额**，以确保推荐结果不会过度集中在某一类热门图书上？
3. **LLM 提示词 (Prompt) 策略**：在“图书评选”任务中，为了体现论文提到的“人文关怀”和“智慧遗珠”，Prompt 中包含了哪些关键的引导语（如关于深度、学术价值、被忽视程度的描述）？

### 模块 2：主题书目推荐与 RSS 分析模块（侧重知识发现与 Agent 协作）

1. **三 Agent 协作的通信机制**：代码中 `ArticleFilter`、`ArticleSummaryAgent` 和 `ArticleAnalyst` 之间是如何进行**数据传递和状态同步**的？后一个 Agent 是否会参考前一个 Agent 的中间结果？
2. **层次聚类算法 (Hierarchical Clustering) 细节**：在进行交叉分析时，代码使用了哪种距离度量方式（如欧氏距离、余弦相似度）？如何自动确定聚类的**停止点（即主题的数量）**？
3. **向量检索 (Vector Retrieval) 实现**：大纲提到了向量检索，但在总结中未详述。请查阅代码：使用的是哪个 **Embedding 模型**？向量数据库（如 FAISS、Chroma 等）是如何与现有的按月聚合 Excel 存储结合使用的？
4. **知识抽取逻辑**：代码如何从非结构化的文章中**提取“提及书籍” (mentioned_books)**？是基于正则匹配、实体识别 (NER) 还是 LLM 的直接提取？

### 模块 3：可视化 Web 模块（侧重 AI 结果呈现）

1. **AI 分析结果的语义展示**：Web 端如何展示第 4 章中提到的“主题关联”和“深度分析报告”？是否有专门的 UI 组件来呈现**文章与图书之间的关联线索**？
2. **动态数据驱动逻辑**：前端在渲染“主题卡”时，是如何解析并展示由模块 2 生成的 `llm_thematic_essence`（主题精要）和 `llm_tags`（标签）的？

### 模块 4：AIBot 智搜系统（侧重知识检索与交互）

1. **意图分类器 (Intent Classifier) 实现**：AIBot 如何识别用户是进行“简单检索”还是需要“深度知识发现”？代码中意图分类的**判定逻辑或 Prompt 依据**是什么？
2. **二次检索 (Iterative Search) 的反馈回路**：在深度检索模式下，系统如何基于第一轮检索到的文章或图书结果，**自动优化**第二轮的检索词以实现“知识深度活化”？
3. **人在环交互 (HITL) 的技术锚点**：在深度检索生成的“检索草稿”中，代码允许用户修改哪些关键参数（如关键词权重、分析维度），这些修改如何反作用于 AI 的最终生成结果？

------

**您可以根据这些问题去阅读代码，补充相关逻辑后，我们将能为第四章提供非常扎实的技术支撑。**

您可以将这一章的逻辑理解为**“智慧采金”中的化学提炼**：如果说之前的降噪筛选是冲洗掉表层的泥沙，那么这一章的 AI 深度评选就是通过精密的“化学反应”（三 Agent 协作与锦标赛算法），从看似普通的矿石中真正析出高纯度的金元素（智慧遗珠）。