# 书海回响 - 模块3-豆瓣评分依赖库
# 作者: 书海回响项目组
# 版本: v1.0
# 日期: 2025-10-30

# 数据处理和Excel操作
pandas>=1.3.0
openpyxl>=3.0.0
xlsxwriter>=3.0.0

# 网络请求和爬虫
requests>=2.25.0
httpx>=0.25.0
beautifulsoup4>=4.9.0
lxml>=4.6.0

# 浏览器自动化（ISBN获取模块需要）
playwright>=1.30.0

# 重试机制
retrying>=1.3.0

# 数据库
# sqlite3 是Python标准库，无需额外安装

# 配置管理
python-dotenv>=0.19.0
pyyaml>=5.4.0

# 大模型调用
openai>=1.40.0
langfuse>=2.0.0

# 日志和时间
python-dateutil>=2.8.0

# 开发和测试工具（可选）
pytest>=6.0.0
pytest-cov>=2.10.0
black>=21.0.0
flake8>=3.8.0

# 性能和并发（可选）
concurrent-futures>=3.1.0
multiprocessing-logging>=0.3.0

# 加密和安全（可选，用于敏感配置）
cryptography>=3.4.0

# 进度条显示（可选）
tqdm>=4.60.0

# HTTP缓存（可选）
requests-cache>=0.5.0

# 数据验证（可选）
cerberus>=1.3.0

# 正则表达式增强（可选）
regex>=2021.4.0
